package com.example.check_in_mobile_app.ui.theme

import androidx.compose.ui.graphics.Color

// Brand
val NavyBlue    = Color(0xFF25316B)   // primary — boutons, headers, accents
val DarkText    = Color(0xFF2A3343)   // texte principal
val DarkText111 = Color(0xFF111827)   // texte très sombre (variante HomeScreen)

// Surfaces & fond
val SurfaceGray   = Color(0xFFF8FAFC) // fond de card légère
val LightGray     = Color(0xFFF3F4F6) // fond bouton secondaire, avatar bg
val DividerColor  = Color(0xFFF1F2F4) // séparateur horizontal
val BorderColor   = Color(0xFFE5E7EB) // bordure input / card
val BorderLight   = Color(0xFFE2E8F0) // bordure card boarding pass
val QrBorder      = Color(0xFFBDC1CA) // bordure QR code

// Texte secondaire
val SubtleText  = Color(0x992A3343)   // labels UPPERCASE, meta-infos
val MediumGray  = Color(0xFF9CA3AF)   // icônes inactives, placeholder

// Statuts
val ActiveGreen   = Color(0xFF34D399) // check-in ouvert / actif
val WarningAmber  = Color(0xFFF59E0B) // boarding en cours
val ErrorRed      = Color(0xFFEF4444) // boarding fermé / erreur

// Overlays (sur fond navy)
val BadgeBg       = Color(0x26FFFFFF) // fond badge "ACTIVE NOW"
val SubtitleWhite = Color(0xBFFFFFFF) // sous-titre sur card bleue